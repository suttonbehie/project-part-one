# This legacy workflow is intentionally disabled.
# The active checkpoint-aware repository checks are in tests.yml.
# Do not rename this file to tests.yml.
name: Legacy Project Tests

on:
  workflow_dispatch:

jobs:
  legacy-tests:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use .github/workflows/tests.yml instead."
